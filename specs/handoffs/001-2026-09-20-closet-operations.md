# Session Handoff - 2026-09-20

## Context
Becca's Closet (static gift site, repo `pianoelias-boop/beccas-closet`, live at https://pianoelias-boop.github.io/beccas-closet/) is in steady-state operation. This long session (2026-09-14 → 2026-09-20) built the site and its automation; the last stretch was operations: the weekly recommendation round, sale detection, return terms, a Shoes shelf for Becca's lymphedema-bandaged right foot, and owner rules for the recommender. Everything below is committed and deployed; nothing is half-done in the working tree.

## Completed (this session, most recent first)
- **Sale-event rule rewritten (2026-09-20)** in `build/sales/check.py`: a store is "running a sale" when a % (≥15) sits next to sitewide/holiday wording anywhere, OR an announcement bar / hero / heading says `N% off` (≥20), `$N off`, or an announcing sale phrase ("shop the sale", "Fall Sale starts now", "sample sale is on"). Nav/footer/form blocks are stripped; blocks with ≥6 links are treated as menus; each phrase is judged in its own ±80-char window; shipping/returns text never vetoes. Owner's stated preference: "I would rather have false positives than miss a sale," but a permanent sale *section* must not count. The checker also now reads each store's sale collection and up to 12 feed pages (Boden's markdowns sat beyond page 6). `sales.yml` has a `dry_run` input that prints the evidence without committing — use it before touching the rule. Live today: Boden 40%, Madewell 40%, Anthropologie 30% (home/gifts promo — owner has not said whether adjacent-category promos bother him), 18 pieces verified marked down.
- **Two owner-requested pieces added** via new `build/add_pieces.py` (retries on HTTP 429): 433 Boden Raglan Embroidered Top (mid blue, $133 on sale from $190), 434 TOAST Washed Black Denim Skirt (carbon black, $280).
- **Recommender rules (2026-09-19)**: never tapered/skinny/cigarette/slim-leg pants, jeans or jumpsuits (she cannot wear tapered legs — right leg in compression); 18 East only for shirts/overshirts (mostly menswear). Enforced in `build/suggest/brands.json` (`rules.avoid_titles`, per-brand `only_titles`), applied in `round.py stage1`, written into `build/suggest/profile.md` and the judge routine's prompt. Round 3's MHL tapered jeans (5032) and 18 East blazer (5034) were retired the same day.
- **Round 3 published**: PR #6 (chosen by Claude) merged by the owner 2026-09-19; 6 pieces live after the two retirements.
- **Weekly round made robust to GitHub cron lag** (GitHub fired this repo's top-of-hour crons 4–5 h late): stage1 cron Sat 04:17 UTC, judge routine (Anthropic scheduler, `trig_01Hn6GFRvidhjxpDNxCsqDdW`) Sat 13:00 UTC, fallback Sat 17:23 UTC, sale check 08:41 UTC daily. If the judge finds `pending/round_meta.json` older than a day it commits `pending/stage1.request` ("Request shortlist") which triggers the stage1 job by push, then polls `git pull` once a minute for up to 25 min. `judged` job keys on the "Round picks" commit message. `round.py stage1` aborts without rewriting files when the notebook or all feeds are unreachable. The cloud sandbox has no internet except GitHub — the routine must never build the shortlist itself and never sends push notifications.
- **Return terms per store** (2026-09-17): `build/returns/retailers.json` → `python3 build/returns/make.py` → `retailers.js` (hourly-stamped loader). Shown as a "Returns at …" line in every item modal and as a store list behind the "40 storefronts" stat button in the About dialog (`#stores`). `build/returns/check.py` runs daily, hashes policy-term sentences, writes `build/returns/changes.json` only — **no issues, no assignments** (owner's rule: he reviews round PRs and nothing else). Six stores block scripts and are on its manual list (J.Crew, Madewell, Ralph Lauren, Converse, Abercrombie, O'Connell's).
- **Shoes shelf** (2026-09-16): ids 422–432 via `build/add_shoes.py`; category `Shoes` added to `CATEGORY_ORDER` in `app.js`; storefront count bumped 36→40. Fit rules for Becca: opening must reach the toe (zip around the toe, straps that peel the vamp back, or a soft moccasin), shaft below the calf, stays on loose (Birkenstock-strap principle). Research brief artifact: https://claude.ai/artifact/7mLr462oyNhCSYx9P4Cwta. Split-size programs cited on listings: BILLY single shoes (email info@billyfootwear.com, single-shoe code), Nordstrom split pair for the price of one when sizes differ ≥1.5 (1-888-282-6060), Cosyfeet strap extensions $9/style, Pedors mis-mate discount. The `note` field is NOT rendered by `app.js`; must-see text goes in `details`.
- **Recolour rule** (2026-09-15): at most one "another colour of a piece she already has" per round (`garment_key` in `round.py`; routine prompt carries the same limit).
- **Standard & Strange** set to `rejected` in `brands.json` (stockist, mostly menswear; store-specific, not a rule about stockists).

## In Progress
- Nothing mid-flight. Two open questions for the owner: (1) should adjacent-category promos like Anthropologie's home/gifts 30% count as a sale event? (2) Levi's is invisible to all automated readers (Akamai challenge blocks scripts, TLS impersonation, reader proxies, and the in-app browser). Verified options: Slickdeals RSS search feed (`https://slickdeals.net/newsearch.php?src=SearchBarV2&q=levi%27s&searcharea=deals&searchin=first&rss=1`) posted the 2026-09-18 Levi's flash sale; a Gmail-label + Apps Script route would be more faithful; a manual override file was also proposed. Owner asked about downsides but has **not** approved any of these.

## Next Steps
1. Watch Saturday 2026-09-26: stage1 ~04:17 UTC (may land hours late), judge 13:00 UTC, fallback 17:23 UTC. A `round` PR should exist by early afternoon ET. Debug with `gh run list --workflow=round.yml` and `RemoteTrigger list_runs` / `get_run_log`.
2. If the owner answers on Levi's: wire the Slickdeals feed into `check.py` as a "reported by deal sites" event (title names brand + sale/%/flash, link points to the brand's domain, ≤48 h old, expire after 48 h), plus the manual note file and a "can't be checked automatically" label for Levi's, O'Connell's, Theory.
3. Parked (owner deferred 2026-09-15): "add a piece by link" feature (page → notebook script → `repository_dispatch` with a repo-scoped token in Script Properties → workflow enriches and commits).
4. Known gaps: 21 Ralph Lauren items still thumbnail-only; Levi's descriptions missing; Ibex return window not stated on their page.

## Key Files
- `app.js`, `index.html`, `styles.css` — site (cache-bust: `data.js?v=5`, `app.js?v=29`, `styles.css?v=21`; `suggestions.js`, `sales.js`, `retailers.js` load with an hourly stamp)
- `build/make_data.py` + `build/extras.json` (ids 396–434) → `data.js`; `build/add_pieces.py` for new Shopify products
- `build/suggest/round.py`, `brands.json` (rules + roster), `profile.md`, `pending/`, `round_*.json`, `retired.json`, `make_suggestions.py` → `suggestions.js`
- `build/sales/check.py` → `sales.js`, `history.json`; `.github/workflows/sales.yml` (daily 08:41 UTC, `dry_run` input)
- `build/returns/retailers.json`, `make.py`, `check.py`, `hashes.json`, `changes.json` → `retailers.js`
- `.github/workflows/round.yml` — jobs `judged` (push "Round picks"), `stage1` (cron 04:17 Sat / dispatch / push "Request shortlist"), `fallback` (cron 17:23 Sat)
- `sync/Code.gs`, `sync/SETUP.md` — the notebook (Google Apps Script; URL in `<meta name="closet-sync">`)
- `README.md`, `build/suggest/README.md` — runbooks
- Memory: `~/.claude/projects/-Users-piano-Documents-Claude-Code/memory/beccas-closet.md` (project) and `subagent-judgment.md` (feedback)

## Blockers / Notes
- **Do not run `check.py` or bulk `.js` probes from this Mac in a burst**: Shopify returned 429 to every store for ~30 min on 2026-09-20. Dispatch `sales.yml` on GitHub instead (`gh workflow run sales.yml -f dry_run=true`).
- **Never load the live site or a local copy pointed at the real notebook during tests**; `app.js` blanks the sync URL on localhost. Verify deploys with curl. Local preview: launch.json config `closet` (port 8768).
- **Subagents**: few and well-aimed, cheaper models (sonnet/haiku) for fetch-and-list, never the default model; keep judging and photo review in the main session (owner feedback after a 12-agent, ~2M-token shoe research round).
- **Owner review surface is round PRs only** — no GitHub issues, no assignments for anything else.
- GitHub cron delays are real and documented; the judge's request-file mechanism is the fix, the off-hour crons are only a mitigation.
- Cloud routine env `env_01TJdAfRMwJ6sD4BTAkvrbHu`, model claude-sonnet-5; it writes only `pending/picks_claude.json` (and `pending/stage1.request`) via the GitHub MCP file tool.
